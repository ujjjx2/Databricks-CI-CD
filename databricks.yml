bundle:
  name: databricks-cicd

targets:
  dev:
    default: true
    workspace:
      host: ${DATABRICKS_HOST}

resources:
  jobs:
    notebook-job:
      name: notebook-job
      tasks:
        - task_key: run-notebook
          notebook_task:
            notebook_path: ./notebooks/
