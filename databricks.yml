bundle:
  name: databricks-ci-cd-project

resources:
  jobs:
    my-job:
      name: my-first-job
      tasks:
        - task_key: task1
          notebook_task:
            notebook_path: ./notebooks/scd.py
